Custom environment variables can be added to __INSTALL_DIR__/.env.local  
See <https://docs.linkwarden.app/self-hosting/environment-variables>  
After changing the .env.local file, run systemctl restart __APP__ to apply the changes.  
